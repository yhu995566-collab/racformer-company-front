"""RaCFormer deployment-only input and inference helpers.

Submodules are intentionally not imported here so schema/buffer users do not
load PyTorch and OpenMMLab until the runner is explicitly imported.
"""
